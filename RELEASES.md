# 0.8.0

* split lexigram full library into two crates:
  * `lexigram-core` minimum required by generated lexer / parser and by lexigram-lib
  * `lexigram-lib` required by lexigram (lexer / parser generator)

# 0.7.0

* refactor crates

# 0.6.0

* add optional span information in listener for each parsed terminal / nonterminal

